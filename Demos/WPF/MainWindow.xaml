<Window x:Class="StellarDS.Demos.WPF.MainWindow" Loaded="Window_Loaded"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StellarDS.Demos.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary Source="DatagridTemplate.xaml"></ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <DataGrid Name="DataGrid" SelectionMode="Single" RowEditEnding="DataGrid_OnRowEditEnding" CanUserDeleteRows="True" Grid.ColumnSpan="2" Grid.RowSpan="4
        " Style="{DynamicResource {x:Type DataGrid}}"></DataGrid>
        <Button Click="ButtonBase_OnClick" Grid.Column="0" Grid.Row="4">Delete Record</Button>
    </Grid>
</Window>
